*vantage.nvim.txt*                    Vantage: coding agents in a tmux-backed terminal


==============================================================================
CONTENTS                                                        *vantage.nvim*

1. Introduction                                      |vantage.nvim-introduction|
2. Requirements                                        |vantage.nvim-requirements|
3. Installation                                        |vantage.nvim-installation|
4. Configuration                                      |vantage.nvim-configuration|
5. Commands                                              |vantage.nvim-commands|
6. Key concepts                                          |vantage.nvim-concepts|

==============================================================================
1. INTRODUCTION                                *vantage.nvim-introduction*

Vantage is a coding-agent manager for Neovim. It runs coding agents (Claude
Code, Codex, dsh, …) in a tmux server and shows them in a single persistent
`:terminal`. tmux is the state store, multiplexer, renderer and input layer —
there is no custom TUI.

The plugin owns the backend: its Lua layer drives a private tmux socket
directly. The frontend is a pluggable picker (native / fzf-lua / snacks) plus
the terminal window that is the tmux client.

==============================================================================
2. REQUIREMENTS                                *vantage.nvim-requirements*

- Neovim >= 0.10
- tmux 3.0+ (developed against 3.6a)
- nvim-treesitter-textobjects (optional; for the `{function}` / `{class}`
  prompt placeholders)

==============================================================================
3. INSTALLATION                                *vantage.nvim-installation*

Install with your favorite package manager. With lazy.nvim: >

    {
      "ZachVec/vantage.nvim",
      cmd  = "Vantage",
      opts = {},
    }

<

lazy.nvim resolves the module name `vantage` from the repo and runs
`require("vantage").setup(opts)` automatically. The plugin loads lazily on the
first `:Vantage`.

==============================================================================
4. CONFIGURATION                              *vantage.nvim-configuration*

`setup()` accepts: >

    require("vantage").setup({
      backend = "tmux",          -- pluggable backend driver (only "tmux" today)
      socket  = "vantage",       -- private tmux socket name
      picker  = "native",        -- native | fzf-lua | snacks
      prompts = {              -- {file}/{line}/{annotations} built in; add/override yours
        ["{file}"] = "{file}",
        ["{line}"] = "{line}",
        ["{annotations}"] = "{annotations}",
      },
      annotations = {          -- notes on line ranges, sent via {annotations}
        item = "{lines} {note}",  -- per-annotation template
        clear_on_send = true,     -- clear after a sent prompt uses {annotations}
        keys = {                  -- note-float keymaps (normal mode)
          exit = "<Esc>",         -- commit the note and close (empty = delete)
          -- delete = "<leader>d", -- optional: delete directly
        },
      },
      cli = {
        tools = {},              -- provide your own (name -> cmd array); nothing built in
        win = {                  -- the terminal window that is the tmux client
          layout = "float",      -- float | left | top | bottom | right
          float  = { width = 0.9, height = 0.9, border = "rounded" },  -- "none" to hide
          split  = { width = 80, height = 20 },
          -- no keymaps by default; add your own (see TERMINAL FILETYPE below)
          keys = {},
        },
      },
    })

<

`cli.tools` is empty by default — provide every tool yourself, e.g. `tools = {
claude = { cmd = { "claude" } } }`. `tools.<name>.cmd` is run verbatim when
creating an Agent; it may include arguments, e.g.
`{ cmd = { "claude", "--dangerously-skip-permissions" } }`. Nothing is built in
or validated. A tool may also carry a `format` function
(`fun(text): string`), applied to a rendered prompt just before it is sent to
the Agent — e.g. to translate the Claude-dialect location references for
another tool.

PROMPT TEMPLATES ~

`prompts` maps names to text templates. Three are built in — `{file}`, `{line}`,
and `{annotations}`, as identity templates (`"{file}"` → `"{file}"`) — so the raw
location references and the accumulated Annotations are always available. The
`{annotations}` prompt is hidden while there are no Annotations. `:Vantage
prompt` picks one via `vim.ui.select` (not the pluggable Picker — there is
nothing to preview) and pastes it into the focused Agent's input (no
auto-submit). Your `prompts` merge additively: a name you set overrides the
built-in, and names you leave unset are kept. A template may use five
placeholders, four of which are Claude-style location references relative to
the Agent's cwd:

  `{file}`         `@path/to/file.lua`
  `{line}`         `@path/to/file.lua :L42` (cursor line)
  `{function}`     `function foo @path/to/file.lua :L42:C3`
  `{class}`        `class Foo @path/to/file.lua :L42:C3`
  `{annotations}`  every Annotation, each rendered by `annotations.item`

`{function}` and `{class}` need nvim-treesitter-textobjects; without it (or
outside a function/class) the prompt is skipped with a warning — that is why
they are not built in. An unknown placeholder is left literal at runtime and
reported by `:checkhealth`. A prompt may span multiple lines; newlines are
typed literally and never submit.
Examples: >

    require("vantage").setup({
      prompts = {
        review    = "Review {file} for issues.",
        fix_line  = "Fix {line}.",
        document  = "Add documentation to {function}.",
      },
    })

<

The Agent's working directory is the current window's local cwd (respects
`:lcd` / `:tcd`), overridable with `create --cwd <dir>`.

ANNOTATIONS ~

An Annotation is a note anchored to a line range in a normal file. Add one
with `:Vantage annotate add` (over the visual selection, else the current
line); `:Vantage annotate` opens a picker of annotations — selecting one jumps
to its range and opens an editable note float. `:Vantage annotate clear`
removes every annotation. Annotations tint the range's line numbers only — no
sign column, no layout shift, no code obscuring; when the number column is off
they are not drawn. They live in memory only: lost on buffer unload/reload or
Neovim exit, and never written to the file.

The note float is a normal scratch buffer (normal mode), so editing is ordinary
Vim — multi-line notes and undo included. The only added keymap is
`annotations.keys.exit` (default `<Esc>`): it commits the note and closes the
float; an empty note deletes the annotation after a confirmation. An optional
`annotations.keys.delete` deletes directly.

The picker is the pluggable one (`native` / `fzf-lua` / `snacks`): fzf-lua and
snacks preview each annotation through `annotations.item` (what you see is what
gets sent) and offer an in-place `<c-x>` delete; native has no preview and no
keymaps, so it is selection only.

Send the accumulated annotations to the focused Agent through a Prompt that
uses `{annotations}`, e.g. `prompts = { notes = "My notes:\n{annotations}" }`.
`annotations.item` is the per-annotation template; its fields are:

  `{note}`    the annotation text
  `{lines}`   `@path/to/file.lua :L10-L20` (`:L10` for a single line)
  `{code}`    the selected lines, leading/trailing blank lines dropped
  `{file}`    the relative path (no `@`, no `:L`)
  `{start}`   the first line number
  `{end}`     the last line number

The default `item` is `"{lines} {note}"`; to also send the code use
`item = "{lines} {note}\n{code}"`. When `clear_on_send` is true (default), a
successful send whose template used `{annotations}` clears all annotations.

PICKERS & PROMPTS ~

Selections go through a pluggable picker, chosen by `picker`:

  `picker = "native"`   built-in `vim.ui.select` (default). Respects any
                        global `vim.ui.select` override (dressing.nvim,
                        snacks' ui_select, …).
  `picker = "fzf-lua"`  fzf-lua; requires the fzf-lua plugin.
  `picker = "snacks"`   snacks.nvim picker; requires snacks.nvim.

`fzf-lua` and `snacks` preview the selected Agent's pane (its recent terminal
output). Free-text prompts (e.g. the new-Group name) use `input()` and are
insert-mode by default.

TERMINAL FILETYPE & KEYMAPS ~

No keymaps are added by default. The terminal buffer has filetype
`vantage_terminal`. Add keys in either of two ways:

1. `cli.win.keys` — a list of 4-tuples `{ lhs, rhs, mode = "n", desc }`,
   applied buffer-locally to the terminal when it is created. `rhs` is passed
   verbatim to `vim.keymap.set` — a key sequence / `<cmd>` RHS, or a Lua
   function. `mode` is `"n"`, `"t"` or `"nt"` (also a table). Example: >

      require("vantage").setup({
        cli = {
          win = {
            keys = {
              { "<c-q>", "<cmd>Vantage toggle<CR>", mode = "t", desc = "toggle the terminal" },
              { "q", "<cmd>Vantage toggle<CR>", mode = "n", desc = "toggle the terminal" },
              { "<c-s>", function() vim.cmd("stopinsert") end, mode = "t", desc = "enter normal mode" },
            },
          },
        },
      })

<

2. Any `ftplugin`/`autocmd` on the filetype (full control, plain `vim.keymap.set`): >

      vim.api.nvim_create_autocmd("FileType", {
        pattern = "vantage_terminal",
        callback = function(ev)
          vim.keymap.set("n", "q", ":Vantage toggle<CR>", { buffer = ev.buf })
          vim.keymap.set("t", "<c-q>", "<cmd>Vantage toggle<CR>", { buffer = ev.buf })
        end,
      })

<

For a normal-mode keymap that opens/toggles from anywhere in nvim (not just
inside the terminal buffer): >

      vim.keymap.set("n", "<leader>vt", "<cmd>Vantage toggle<CR>", { desc = "Toggle Vantage" })

<

==============================================================================
5. COMMANDS                                    *vantage.nvim-commands*

                                                          *:Vantage*
`:Vantage`
    Show help (no default action).

`:Vantage switch [<@N>]`
    Switch: pick an Agent to focus, or pick the `+ new agent` entry to create
    one (choose a tool, then a Group). With `<@N>` focus directly.

`:Vantage kill [<group|@N>]`
    Kill a Group or a single Agent. With no argument, pick interactively.

`:Vantage toggle`
    Hide the terminal window if it is open, or show it again — a lightweight
    toggle that keeps the buffer, tmux client and View alive. If there is no
    live terminal, re-open to the last-focused Agent; if there are no Agents at
    all, start the create wizard. Map it, e.g. `vim.keymap.set("n",
    "<leader>vt", ":Vantage toggle<CR>")`.

`:Vantage detach`
    Detach the terminal's client (the client-detached hook destroys the View).
    Agents keep running headless.

`:Vantage prompt`
    Pick a Prompt and type it into the focused Agent's input (renders
    `{file}` / `{line}` / `{function}` / `{class}` / `{annotations}`; no
    auto-submit). Warns when there is no focused Agent.

`:Vantage annotate`
    Open the Annotation picker (pluggable; fzf-lua/snacks preview each note and
    offer an in-place `<c-x>` delete). Selecting one jumps to its range and
    opens an editable note float.

`:Vantage annotate add`
    Add an Annotation over the command's range — a visual selection, or the
    current line when none — and open an empty note float.

`:Vantage annotate clear`
    Remove every Annotation (after a confirmation).

`:Vantage status`
    Show attached clients and sessions (debug).

==============================================================================
6. KEY CONCEPTS                                  *vantage.nvim-concepts*

Group ~
    A tmux session group: one persistent Anchor session owns the Agents.

Anchor ~
    The one persistent session in a Group that keeps its Agents alive headless.

Agent ~
    A tmux window running a coding agent, shared across a Group's Views.

View ~
    A transient grouped session attached by a client. Closing the terminal
    detaches the client, the client-detached hook destroys the View, and the
    Agents keep running.

==============================================================================
vim:tw=78:ts=8:noet:ft=help:norl:
